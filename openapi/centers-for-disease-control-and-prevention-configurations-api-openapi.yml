openapi: 3.2.0
info:
  title: DIBBs eCR Refiner Configurations API
  description: 'Please visit the repo for more info: https://github.com/CDCgov/dibbs-ecr-refiner'
  version: 1.0.0
tags:
- name: Configurations
paths:
  /api/v1/configurations/:
    get:
      tags:
      - Configurations
      summary: Get Configurations
      description: 'Returns a list of configurations based on the logged-in user.


        Returns:

        List of configuration objects.'
      operationId: getConfigurations
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/GetConfigurationsResponse'
                type: array
                title: Response Getconfigurations
    post:
      tags:
      - Configurations
      summary: Create Configuration
      description: Create a new configuration for a jurisdiction.
      operationId: createConfiguration
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateConfigInput'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateConfigurationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/serialized:
    get:
      tags:
      - Configurations
      summary: Get Serialized Configuration
      description: 'Given an active configuration ID, fetches and returns the serialized configuration file content from S3.


        Args:

        configuration_id (UUID): The active configuration ID

        user (DbUser): The logged-in user

        app_config (AppConfig): The required application environment variables

        db (AsyncDatabaseConnection): The database connection

        logger (Logger): The standard app logger


        Raises:

        HTTPException: 403 if not running locally

        HTTPException: 404 if configuration cannot be found in the user''s jurisdiction

        HTTPException: 400 if the configuration''s status is not `active`

        HTTPException: 404 if the configuration''s primary condition is not found


        Returns:

        Response: The serialized configuration file content'
      operationId: getSerializedConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SerializedFiles'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}:
    get:
      tags:
      - Configurations
      summary: Get Configuration
      description: Get a single configuration by its ID including all associated conditions.
      operationId: getConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetConfigurationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/code-sets:
    put:
      tags:
      - Configurations
      summary: Associate Condition Codeset With Configuration
      description: 'Associate a specified code set with the given configuration.


        Args:

        configuration_id (UUID): ID of the configuration

        body (AssociateCodesetInput): payload containing a condition_id

        user (dict[str, Any], optional): User making the request

        db (AsyncDatabaseConnection, optional): Database connection


        Raises:

        HTTPException: 404 if configuration is not found in JD

        HTTPException: 404 if configuration is not found

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if configuration cannot be updated


        Returns:

        AssociateCodesetResponse: ID of updated configuration, the full list of included conditions,

        and the condition_name'
      operationId: associateConditionWithConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssociateCodesetInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssociateCodesetResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/code-sets/{condition_id}:
    delete:
      tags:
      - Configurations
      summary: Remove Condition Codeset From Configuration
      description: 'Remove a specified code set from the given configuration.


        Args:

        configuration_id (UUID): ID of the configuration

        condition_id (UUID): ID of the condition to remove

        user (DbUser): User making the request

        db (AsyncDatabaseConnection): Database connection


        Raises:

        HTTPException: 404 if configuration is not found in JD

        HTTPException: 404 if condition is not found

        HTTPException: 409 if trying to remove the main condition

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if configuration is cannot be updated


        Returns:

        AssociateCodesetResponse: ID of updated configuration and the full list

        of included conditions plus condition_name'
      operationId: disassociateConditionWithConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      - name: condition_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Condition Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssociateCodesetResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/custom-codes/{id}:
    get:
      tags:
      - Configurations
      summary: Get Custom Code
      description: 'Fetch a custom code by its ID.


        Args:

        configuration_id (UUID): The associated configuration ID

        id (UUID): The custom code ID

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): The database connection


        Raises:

        HTTPException: 404 if configuration can''t be found


        Returns:

        CustomCodeResponse: The custom code response object'
      operationId: getCustomCode
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomCodeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Configurations
      summary: Delete Custom Code
      description: 'Delete a custom code from a configuration.


        Args:

        configuration_id (UUID): The ID of the configuration to modify.

        id (str): The ID of the custom code.

        user (dict[str, Any]): The logged-in user.

        db (AsyncDatabaseConnection): The database connection.


        Raises:

        HTTPException: 400 if id is not provided

        HTTPException: 404 if configuration can''t be found

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if configuration can''t be updated


        Returns:

        ConfigurationCustomCodeResponse: The updated configuration'
      operationId: deleteCustomCodeFromConfiguration
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Id
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomCodeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/custom-codes:
    post:
      tags:
      - Configurations
      summary: Add Custom Code
      description: 'Add a user-defined custom code to a configuration.


        Args:

        configuration_id (UUID): The ID of the configuration to update.

        body (AddCustomCodeInput): The custom code information provided by the user.

        user (dict[str, Any]): The logged-in user.

        db (AsyncDatabaseConnection): The database connection.


        Raises:

        HTTPException: 404 if configuration isn''t found

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if custom code can''t be added


        Returns:

        ConfigurationCustomCodeResponse: Updated configuration'
      operationId: addCustomCodeToConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCustomCodeInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomCodeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - Configurations
      summary: Edit Custom Code
      description: 'Modify a configuration''s custom code based on system_key/code pair.


        Args:

        configuration_id (UUID): The ID of the configuration to modify.

        body (UpdateCustomCodeInput): User-provided object containing custom code info.

        user (dict[str, Any]): The logged-in user.

        db (AsyncDatabaseConnection): The database connection.

        logger (Logger): The system logger.


        Raises:

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if the configuration can''t be updated


        Returns:

        ConfigurationCustomCodeResponse: The updated configuration.'
      operationId: editCustomCodeFromConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCustomCodeInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomCodeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/custom-codes/upload:
    post:
      tags:
      - Configurations
      summary: Upload Custom Codes Csv
      description: 'Accepts a CSV payload in JSON body.


        Expected CSV headers:

        code,code_system,display_name


        Returns:

        UploadCustomCodesResponse'
      operationId: uploadCustomCodesCsv
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UploadCustomCodesCsvInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadCustomCodesPreviewResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/custom-codes/confirm:
    post:
      tags:
      - Configurations
      summary: Confirm Upload Custom Codes Csv
      description: Confirm and save custom codes from preview list.
      operationId: confirmUploadCustomCodesCsv
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmUploadCustomCodesInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CustomCodeResponse'
                title: Response Confirmuploadcustomcodescsv
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/custom-codes/bulk-delete:
    post:
      tags:
      - Configurations
      summary: Bulk Delete Custom Codes
      description: 'Deletes custom codes in bulk for a given configuration.


        Args:

        configuration_id (UUID): The ID of the configuration to modify.

        body (BulkDeleteCustomCodesInput): The input body containing IDs of the custom codes.

        user (DbUser): The logged-in user.

        db (AsyncDatabaseConnection): The database connection.


        Raises:

        HTTPException: 404 if configuration can''t be found

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if configuration can''t be updated


        Returns:

        ConfigurationCustomCodeResponse: The updated configuration'
      operationId: deleteCustomCodes
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkDeleteCustomCodesInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CustomCodeResponse'
                title: Response Deletecustomcodes
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/custom-codes/validate:
    post:
      tags:
      - Configurations
      summary: Validate Custom Code
      description: 'Determines whether a custom code update is valid or not.


        If the desired code is already associated with the configuration, then the update is

        invalid.


        Args:

        configuration_id (UUID): The configuration ID

        body (ValidateCustomCodeInput): Body including the code to validate

        user (DbUser, optional): The logged in user

        db (AsyncDatabaseConnection, optional): The database connection


        Returns:

        bool: Returns True if the code has not been used, otherwise returns False'
      operationId: validateCustomCodeFromConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidateCustomCodeInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidateCustomCodeResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/export:
    get:
      tags:
      - Configurations
      summary: Get Configuration Export
      description: Create a CSV export of a configuration and all associated codes.
      operationId: getConfigurationExport
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/test:
    post:
      tags:
      - Configurations
      summary: Run Configuration Test
      description: 'Runs an inline test of a given configuration against an eICR/RR pair.


        This endpoint orchestrates the validation and refinement process by:

        1. Handling file input, either from a user upload or a default sample file.

        2. Calling the `inline_testing` service, which validates that the specified

        configuration''s condition is reportable in the provided file.

        3. Handling the service response:

        - If validation fails, raises a 400 Bad Request with a specific error.

        - If successful, proceeds with the returned refined document.

        4. Packaging the original eICR, RR, and the single refined eICR into a

        new in-memory zip archive.

        5. Uploading the archive to S3 and generating a pre-signed download URL.

        6. Returning a `ConfigurationTestResponse` with the download URL and details

        of the successful refinement.


        Args:

        id: The ID of the configuration to test.

        uploaded_file: An optional user-provided zip file with an eICR and RR.

        create_output_zip: Dependency to create a zip archive in memory.

        upload_zip: Dependency to upload the archive to S3.

        user: The authenticated user making the request.

        db: The database connection.

        sample_zip_path: Path to the default sample zip file.

        logger: The application logger.


        Returns:

        A response object containing the original eICR, a URL to download the

        zipped results, and details about the refined condition.'
      operationId: runInlineConfigurationTest
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_runInlineConfigurationTest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigurationTestResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/sections:
    post:
      tags:
      - Configurations
      summary: Insert Custom Section
      description: 'Create a new custom section for a given configuration ID.


        Args:

        configuration_id (UUID): The ID of the configuration

        section_input (CustomSectionInput): Desired properties of the section

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): The database connection'
      operationId: addCustomSection
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddSectionInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: string
                title: Response Addcustomsection
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - Configurations
      summary: Delete Custom Section
      description: 'Delete a custom section.


        Args:

        configuration_id (UUID): ID of the configuration with custom section to delete

        section_input (DeleteCustomSectionInput): Custom section deletion input

        user (DbUser): The logged in user

        db (AsyncDatabaseConnection): The database connection


        Raises:

        HTTPException: 404 if configuration isn''t found

        HTTPException: 409 if configuration isn''t a draft

        HTTPException: 404 if custom section code to delete isn''t found


        Returns:

        str: Deleted custom section code'
      operationId: deleteCustomSection
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteSectionInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: string
                title: Response Deletecustomsection
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    patch:
      tags:
      - Configurations
      summary: Update Section
      description: 'Update a section entry for a configuration.


        Args:

        configuration_id (UUID): ID of the configuration to update

        section_input (SectionUpdateInput): Updated section info

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): Database connection


        Raises:

        HTTPException: 400 if the code is not valid, code is in use,

        name is in use, the section is system-skipped

        (`DisabledSection`) and therefore not configurable, the

        narrative/action combination is unsupported (e.g.

        narrative="reconstruct" or "keep_on_match" with

        action="retain"), narrative "reconstruct" targets a

        section without a registered reconstructor, or the

        section can carry a trigger code and was sent with

        include=False

        HTTPException: 404 if configuration isn''t found

        HTTPException: 409 if configuration is not a draft and therefore not editable

        HTTPException: 500 if section processing can''t be updated


        Returns:

        UpdateSectionProcessingResponse: The message to show the user'
      operationId: updateSection
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SectionUpdateInput'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateSectionProcessingResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/activate:
    patch:
      tags:
      - Configurations
      summary: Activate Configuration
      description: 'Activate the specified configuration.


        Args:

        configuration_id (UUID): ID of the configuration to update

        user (DbUser): The logged-in user

        logger (Logger): The standard logger

        db (AsyncDatabaseConnection): Database connection


        Raises:

        HTTPException: 400 if configuration can''t be activated because of its current state

        HTTPException: 404 if configuration can''t be found

        HTTPException: 500 if configuration can''t be activated by the server


        Returns:

        ActivateConfigurationResponse: Metadata about the activated condition for confirmation'
      operationId: activateConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigurationStatusUpdateResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/deactivate:
    patch:
      tags:
      - Configurations
      summary: Deactivate Configuration
      description: 'Deactivate the specified configuration.


        Args:

        configuration_id (UUID): ID of the configuration to update

        user (DbUser): The logged-in user

        logger (Logger): The standard application logger

        db (AsyncDatabaseConnection): Database connection


        Raises:

        HTTPException: 400 if configuration can''t be deactivated because of its current state

        HTTPException: 404 if configuration can''t be found

        HTTPException: 500 if configuration can''t be deactivated by the server


        Returns:

        ConfigurationStatusUpdateResponse: Metadata about the activated condition for confirmation'
      operationId: deactivateConfiguration
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigurationStatusUpdateResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/acquire-lock:
    post:
      tags:
      - Configurations
      summary: Acquire Configuration Lock
      description: 'Acquire config lock for user.


        Args:

        configuration_id (UUID): ID of the configuration to lock

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): Database connection


        Returns:

        Response: 204 No Content on success'
      operationId: acquireConfigurationLock
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/release-lock:
    post:
      tags:
      - Configurations
      summary: Release Configuration Lock
      description: 'Release config lock if held by user.


        Args:

        configuration_id (UUID): ID of the configuration to update

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): Database connection'
      operationId: releaseConfigurationLock
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/set-status:
    post:
      tags:
      - Configurations
      summary: Set Codes Status
      description: 'Sets selected codes to the specified `status` for the given configuration ID.


        If `update_beyond_rendered_set` is false, we update status for only the specified

        `code_ids` within the rendered page.


        If `update_beyond_rendered_set` is true, we skip any codes within `code_ids_to_skip`

        and update status for all other codes that don''t get clipped away by the passed-in filters


        Args:

        configuration_id (UUID): ID of the configuration to update

        update_beyond_rendered_set (bool): Whether the action should be only within the rendered codes or include all codes.

        code_ids (list[UUID]): List of code IDs to specifically action. Used in the "within cursor" flow.

        code_ids_to_skip (list[UUID]): List of code IDs to skip since they''ve been manually actioned by the user.

        filters (FilterInput): Filter input coming from the client to build the "complete" set of codes to bulk select

        status (Literal[''included'', ''excluded''): Set codes as ''included'' or ''excluded''

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): Database connection


        Raises:

        HTTPException: 404 if configuration can''t be found


        Returns:

        list[UUID]: Code IDs that had their status changed'
      operationId: setCodesStatus
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      - name: update_beyond_rendered_set
        in: query
        required: true
        schema:
          type: boolean
          title: Update Beyond Rendered Set
      - name: status
        in: query
        required: true
        schema:
          enum:
          - included
          - excluded
          type: string
          title: Status
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: code_systems
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          default: []
          title: Code Systems
      - name: sources
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          default: []
          title: Sources
      - name: statuses
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          default: []
          title: Statuses
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Body_setCodesStatus'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
                  format: uuid
                title: Response Setcodesstatus
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/codes:
    get:
      tags:
      - Configurations
      summary: Get Codes
      description: 'Fetches all codes associated with a configuration.


        Args:

        configuration_id (UUID): ID of the configuration to update

        filters (FilterInput): Filter input coming from the client

        cursor (str | None): The cursor for the page to start from

        user (DbUser): The logged-in user

        logger (Logger): The standard logger

        db (AsyncDatabaseConnection): Database connection'
      operationId: getCodes
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      - name: cursor
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Cursor
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: code_systems
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          default: []
          title: Code Systems
      - name: sources
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          default: []
          title: Sources
      - name: statuses
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
          default: []
          title: Statuses
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CodesResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/code-counts:
    get:
      tags:
      - Configurations
      summary: Get Code Counts
      description: 'Fetch code count information for a configuration.


        Args:

        configuration_id (UUID): ID of the configuration to update

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): Database connection


        Raises:

        HTTPException: 404 if configuration can''t be found

        HTTPException: 500 if code count metadata can''t be fetched


        Returns:

        CodeCountsResponse: Object containing code count info'
      operationId: getCodeCounts
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CodeCountsResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/configurations/{configuration_id}/filters:
    get:
      tags:
      - Configurations
      summary: Get Code Filters
      description: 'Fetches code filter information for the client to display.


        Args:

        configuration_id (UUID): The configuration ID

        user (DbUser): The logged-in user

        db (AsyncDatabaseConnection): The database connection


        Raises:

        HTTPException: 404 if the configuration couldn''t be found


        Returns:

        CodeFilterOptions: The code filters'
      operationId: getCodeFilters
      parameters:
      - name: configuration_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Configuration Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CodeFilterOptions'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    IncludedCondition:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        display_name:
          type: string
          title: Display Name
        canonical_url:
          type: string
          title: Canonical Url
        version:
          type: string
          title: Version
        associated:
          type: boolean
          title: Associated
        code_set_status:
          $ref: '#/components/schemas/CodeSetStatus'
      type: object
      required:
      - id
      - display_name
      - canonical_url
      - version
      - associated
      - code_set_status
      title: IncludedCondition
      description: Model for a condition that is associated with a configuration.
    NarrativeOnlySection:
      type: string
      enum:
      - 10154-3
      - 29299-5
      - 10164-2
      - 10187-3
      title: NarrativeOnlySection
      description: 'These sections have no entry match rules in the eICR specification.


        They are conveyed via the narrative block only. Configuring them for

        "refine" is meaningless (there is nothing to match against), so the UI

        disables the refine toggle for them and the refinement plan normalizes

        "refine" -> "retain" for these codes (see refine.py). The Enum values

        remain the single source of truth shipped to the frontend. A unit test

        guards that this enum stays in sync with the spec catalog (every code

        listed here has has_match_rules=False in the catalog, and every catalog

        section with has_match_rules=False is listed here).'
    AddCustomCodeInput:
      properties:
        code:
          type: string
          title: Code
        display:
          type: string
          title: Display
        system_id:
          type: string
          format: uuid
          title: System Id
      type: object
      required:
      - code
      - display
      - system_id
      title: AddCustomCodeInput
      description: Input model for adding a custom code to a configuration.
    UpdateSectionProcessingResponse:
      properties:
        section_updated_code:
          type: string
          title: Section Updated Code
        disabled_section:
          items:
            $ref: '#/components/schemas/DisabledSection'
          type: array
          title: Disabled Section
        narrative_only_section:
          items:
            $ref: '#/components/schemas/NarrativeOnlySection'
          type: array
          title: Narrative Only Section
        reconstructable_section:
          items:
            $ref: '#/components/schemas/ReconstructableSection'
          type: array
          title: Reconstructable Section
        trigger_code_section:
          items:
            $ref: '#/components/schemas/TriggerCodeSection'
          type: array
          title: Trigger Code Section
        narrative_data_labels:
          $ref: '#/components/schemas/NarrativeDataLabels'
        coded_data_labels:
          $ref: '#/components/schemas/CodedDataLabels'
      type: object
      required:
      - section_updated_code
      - disabled_section
      - narrative_only_section
      - reconstructable_section
      - trigger_code_section
      - narrative_data_labels
      - coded_data_labels
      title: UpdateSectionProcessingResponse
      description: Section with information for a custom section update.
    UploadCustomCodesPreviewResponse:
      properties:
        preview_items:
          items:
            $ref: '#/components/schemas/UploadCustomCodesPreviewItem'
          type: array
          title: Preview Items
        codes_processed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Codes Processed
        total_custom_codes_in_configuration:
          anyOf:
          - type: integer
          - type: 'null'
          title: Total Custom Codes In Configuration
        code_systems:
          items:
            $ref: '#/components/schemas/DbCodeSystem'
          type: array
          title: Code Systems
      type: object
      required:
      - preview_items
      - code_systems
      title: UploadCustomCodesPreviewResponse
      description: Validated CSV preview for delayed confirmation; only valid if preview.
    GetConfigurationsResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        status:
          $ref: '#/components/schemas/DbConfigurationStatus'
      type: object
      required:
      - id
      - name
      - status
      title: GetConfigurationsResponse
      description: Model for a user-defined configuration.
    CustomCodeResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        display:
          type: string
          title: Display
        code:
          type: string
          title: Code
        system_id:
          type: string
          format: uuid
          title: System Id
        system_name:
          type: string
          title: System Name
      type: object
      required:
      - id
      - display
      - code
      - system_id
      - system_name
      title: CustomCodeResponse
      description: Custom code object to return to the client.
    CodeCountsResponse:
      properties:
        total_code_count:
          type: integer
          title: Total Code Count
        total_code_sets_count:
          type: integer
          title: Total Code Sets Count
        total_excluded_codes_count:
          type: integer
          title: Total Excluded Codes Count
        total_custom_codes_count:
          type: integer
          title: Total Custom Codes Count
        primary_condition_rctc_count:
          type: integer
          title: Primary Condition Rctc Count
      type: object
      required:
      - total_code_count
      - total_code_sets_count
      - total_excluded_codes_count
      - total_custom_codes_count
      - primary_condition_rctc_count
      title: CodeCountsResponse
      description: Code count information to return to the client.
    DisabledSection:
      type: string
      enum:
      - 83910-0
      - 88085-6
      title: DisabledSection
      description: "These sections are preserved intact in every refined document.\n\nThey sit outside the normal refinement workflow not because the\nrefiner needs to protect them, but because we don't yet have a\nreal-world authoring contract to refine against:\n\n- Reportability Response Information (88085-6) is defined by the\n  eICR STU 3.1.1 IG as a section the PHA populates after eICR\n  receipt, as part of internal data integration -- not something\n  the HCO authors at eICR generation time. It's sketched in the\n  IG but is not part of eICRs flowing through AIMS today.\n\n- Emergency Outbreak Information (83910-0) has a defined section\n  template and a deliberately generic Observation structure\n  (\"unknown until the time of the outbreak,\" per the IG). There\n  is no settled EHR implementation pattern, and the next outbreak\n  will likely shape how this section actually appears in\n  production.\n\nSkipping is the easier path for now and produces Schematron-valid\noutput. We can revisit -- per section, independently -- if and\nwhen either becomes something we actually see in real documents.\n\nThe Enum is the single source of truth: used at runtime to derive\nSECTION_PROCESSING_SKIP, and at the API boundary so Orval ships\nthe concrete LOINC codes to the frontend as const values rather\nthan as plain `string[]`."
    Body_setCodesStatus:
      properties:
        code_ids_to_skip:
          items:
            type: string
            format: uuid
          type: array
          title: Code Ids To Skip
        code_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Code Ids
      type: object
      required:
      - code_ids_to_skip
      - code_ids
      title: Body_setCodesStatus
    CodesLimitResponse:
      properties:
        codes_limit:
          type: integer
          const: 100
          title: Codes Limit
          default: 100
      type: object
      title: CodesLimitResponse
      description: Utility class to help Orval ship these values to the frontend.
    SerializedFile:
      properties:
        key:
          type: string
          title: Key
        content:
          type: string
          title: Content
      type: object
      required:
      - key
      - content
      title: SerializedFile
      description: An individual serialized file.
    AddSectionInput:
      properties:
        code:
          type: string
          title: Code
        name:
          type: string
          title: Name
      type: object
      required:
      - code
      - name
      title: AddSectionInput
      description: Request body for adding a section.
    GetConfigurationResponseVersion:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        version:
          type: integer
          title: Version
        condition_canonical_url:
          type: string
          title: Condition Canonical Url
        status:
          $ref: '#/components/schemas/DbConfigurationStatus'
        created_at:
          type: string
          format: date-time
          title: Created At
        created_by:
          type: string
          title: Created By
        last_activated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Activated At
        last_activated_by:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Activated By
      type: object
      required:
      - id
      - version
      - condition_canonical_url
      - status
      - created_at
      - created_by
      - last_activated_at
      - last_activated_by
      title: GetConfigurationResponseVersion
      description: Model representing a version of a configuration.
    Condition:
      properties:
        code:
          type: string
          title: Code
        display_name:
          type: string
          title: Display Name
        refined_eicr:
          type: string
          title: Refined Eicr
        stats:
          items:
            type: string
          type: array
          title: Stats
        render_diff:
          type: boolean
          title: Render Diff
      type: object
      required:
      - code
      - display_name
      - refined_eicr
      - stats
      - render_diff
      title: Condition
      description: Model for a Condition.
    DbCodeSystem:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        key:
          type: string
          title: Key
        display_name:
          type: string
          title: Display Name
        oid:
          type: string
          title: Oid
      type: object
      required:
      - id
      - key
      - display_name
      - oid
      title: DbCodeSystem
      description: A code system row from the `systems` table.
    ConfigurationStatusUpdateResponse:
      properties:
        configuration_id:
          type: string
          format: uuid
          title: Configuration Id
        status:
          $ref: '#/components/schemas/DbConfigurationStatus'
      type: object
      required:
      - configuration_id
      - status
      title: ConfigurationStatusUpdateResponse
      description: Response model for updating the status a configuration.
    DbSectionType:
      type: string
      enum:
      - standard
      - custom
    CodeFilterOptions:
      properties:
        code_systems:
          items:
            $ref: '#/components/schemas/CodeSystemFilterOption'
          type: array
          title: Code Systems
        sources:
          items:
            $ref: '#/components/schemas/SourceFilterOption'
          type: array
          title: Sources
        statuses:
          items:
            $ref: '#/components/schemas/StatusFilterOption'
          type: array
          title: Statuses
      type: object
      required:
      - code_systems
      - sources
      - statuses
      title: CodeFilterOptions
      description: Model to represent all filter options available to the client.
    ValidateCustomCodeResponse:
      properties:
        valid:
          type: boolean
          title: Valid
      type: object
      required:
      - valid
      title: ValidateCustomCodeResponse
      description: Validation response model.
    CodedDataLabels:
      properties:
        retain:
          type: string
          const: Keep original
          title: Retain
          default: Keep original
        refine:
          type: string
          const: Refine
          title: Refine
          default: Refine
      type: object
      title: CodedDataLabels
      description: Enum class to type the narrative actions possible for the frontend.
    SourceFilterOption:
      properties:
        condition_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Condition Id
        source:
          type: string
          title: Source
        code_count:
          type: integer
          title: Code Count
      type: object
      required:
      - condition_id
      - source
      - code_count
      title: SourceFilterOption
      description: Model to represent a source filter option.
    ConfigurationTestResponse:
      properties:
        original_eicr:
          type: string
          title: Original Eicr
        refined_download_key:
          type: string
          title: Refined Download Key
        condition:
          $ref: '#/components/schemas/Condition'
      type: object
      required:
      - original_eicr
      - refined_download_key
      - condition
      title: ConfigurationTestResponse
      description: Model to represent the response provided to the client when in-line testing is run.
    DbSectionAction:
      type: string
      enum:
      - retain
      - refine
    DeleteSectionInput:
      properties:
        code:
          type: string
          title: Code
      type: object
      required:
      - code
      title: DeleteSectionInput
      description: Request body to delete a section.
    BulkDeleteCustomCodesInput:
      properties:
        ids:
          items:
            type: string
            format: uuid
          type: array
          title: Ids
        ids_to_skip:
          items:
            type: string
            format: uuid
          type: array
          title: Ids To Skip
        delete_all:
          type: boolean
          title: Delete All
      type: object
      required:
      - ids
      - ids_to_skip
      - delete_all
      title: BulkDeleteCustomCodesInput
      description: Input model for a bulk custom codes deletion request.
    Body_runInlineConfigurationTest:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        uploaded_file:
          anyOf:
          - type: string
            contentMediaType: application/octet-stream
          - type: 'null'
          title: Uploaded File
      type: object
      required:
      - id
      title: Body_runInlineConfigurationTest
    AssociateCodesetInput:
      properties:
        condition_id:
          type: string
          format: uuid
          title: Condition Id
      type: object
      required:
      - condition_id
      title: AssociateCodesetInput
      description: Condition association input model.
    CreateConfigurationResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
      type: object
      required:
      - id
      - name
      title: CreateConfigurationResponse
      description: Configuration creation response model.
    ReconstructableSection:
      type: string
      enum:
      - 30954-2
      - 11450-4
      - 11369-6
      - 29549-3
      - 18776-5
      title: ReconstructableSection
      description: 'These sections support the "reconstruct" narrative action.


        Results, Problems, Immunizations, Medications Administered, and Plan of

        Treatment are enabled. Make sure to update unit tests to ensure only

        certain sections are reconstructable.'
    CodeSystemFilterOption:
      properties:
        system_id:
          type: string
          format: uuid
          title: System Id
        system_name:
          type: string
          title: System Name
        code_count:
          type: integer
          title: Code Count
      type: object
      required:
      - system_id
      - system_name
      - code_count
      title: CodeSystemFilterOption
      description: Model to represent a code system filter option.
    DbCode:
      properties:
        code:
          type: string
          title: Code
        display:
          type: string
          title: Display
        system_id:
          type: string
          format: uuid
          title: System Id
        system_name:
          type: string
          title: System Name
      type: object
      required:
      - code
      - display
      - system_id
      - system_name
      title: DbCode
      description: Model for all coded information stored in the codes table.
    ConfirmUploadCustomCodesInput:
      properties:
        custom_codes:
          items:
            $ref: '#/components/schemas/AddCustomCodeInput'
          type: array
          title: Custom Codes
      type: object
      required:
      - custom_codes
      title: ConfirmUploadCustomCodesInput
      description: Payload used to confirm a previously validated CSV import.
    CodeSetStatus:
      type: string
      enum:
      - not expanded
      - partially complete
      - fully complete
    DbConfigurationStatus:
      type: string
      enum:
      - draft
      - inactive
      - active
    DbTotalConditionCodeCount:
      properties:
        condition_id:
          type: string
          format: uuid
          title: Condition Id
        display_name:
          type: string
          title: Display Name
        total_codes:
          type: integer
          title: Total Codes
      type: object
      required:
      - condition_id
      - display_name
      - total_codes
      title: DbTotalConditionCodeCount
      description: Total code count model.
    TriggerCodeSection:
      type: string
      enum:
      - 10160-0
      - 11369-6
      - 11450-4
      - 18776-5
      - 29549-3
      - 30954-2
      - 42346-7
      - 46240-8
      - 47519-4
      title: TriggerCodeSection
      description: 'These sections can carry an eICR trigger code template.


        The eICR IG defines trigger code templates for these sections, so a

        trigger code — the coded evidence of *why* the document was

        generated — can appear in any of them. Removing a section strips

        every `<entry>` it holds and marks it `nullFlavor="NI"` (see

        `create_minimal_section`), so a jurisdiction that turned all of

        these off would emit a document with no trigger codes anywhere and

        fail Schematron validation.


        In practice this is unlikely: nearly all RCTC codes from the eRSD

        are carried in the reporting specification groupers, and the

        additional context groupers widen that further, so a configuration

        would normally match the trigger code and keep the section. This

        policy exists to close the foot-gun, not because we expect

        jurisdictions to walk into it.


        The refiner therefore forces `include=True` for these sections.

        Every other setting stays under jurisdiction control — coded data

        may still be refined or retained, and the narrative may be

        retained, removed, kept on match, or reconstructed.


        Membership is IG-derived, not a judgement call: the codes here are

        the union of `specification.get_trigger_code_sections()` across

        every supported eICR version. The union matters because a

        configuration is authored once and applied to whichever version

        arrives. The Enum is spelled out rather than computed so Orval

        ships concrete LOINC codes to the frontend, and so a change to the

        IG manifest surfaces as a failing drift test rather than silently

        relaxing every jurisdiction''s configuration. A unit test guards

        that this enum stays in sync with the specification.'
    CodesResponse:
      properties:
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
        codes:
          items:
            $ref: '#/components/schemas/CodeResponse'
          type: array
          title: Codes
        codes_limit:
          $ref: '#/components/schemas/CodesLimitResponse'
      type: object
      required:
      - next_cursor
      - codes
      - codes_limit
      title: CodesResponse
      description: Codes and metadata to return to the client.
    GetConfigurationResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        draft_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Draft Id
        is_draft:
          type: boolean
          title: Is Draft
        condition_id:
          type: string
          format: uuid
          title: Condition Id
        condition_canonical_url:
          type: string
          title: Condition Canonical Url
        display_name:
          type: string
          title: Display Name
        status:
          $ref: '#/components/schemas/DbConfigurationStatus'
        code_sets:
          items:
            $ref: '#/components/schemas/DbTotalConditionCodeCount'
          type: array
          title: Code Sets
        included_conditions:
          items:
            $ref: '#/components/schemas/IncludedCondition'
          type: array
          title: Included Conditions
        custom_codes:
          items:
            $ref: '#/components/schemas/CustomCodeResponse'
          type: array
          title: Custom Codes
        section_processing:
          items:
            $ref: '#/components/schemas/DbConfigurationSectionProcessing'
          type: array
          title: Section Processing
        all_versions:
          items:
            $ref: '#/components/schemas/GetConfigurationResponseVersion'
          type: array
          title: All Versions
        rsg_codes:
          items:
            $ref: '#/components/schemas/DbCode'
          type: array
          title: Rsg Codes
        version:
          type: integer
          title: Version
        active_configuration_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Active Configuration Id
        active_version:
          anyOf:
          - type: integer
          - type: 'null'
          title: Active Version
        latest_version:
          type: integer
          title: Latest Version
        is_locked:
          type: boolean
          title: Is Locked
        locked_by:
          anyOf:
          - $ref: '#/components/schemas/LockedByUser'
          - type: 'null'
      type: object
      required:
      - id
      - draft_id
      - is_draft
      - condition_id
      - condition_canonical_url
      - display_name
      - status
      - code_sets
      - included_conditions
      - custom_codes
      - section_processing
      - all_versions
      - rsg_codes
      - version
      - active_configuration_id
      - active_version
      - latest_version
      - is_locked
      - locked_by
      title: GetConfigurationResponse
      description: Model for a configuration response.
    LockedByUser:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        email:
          type: string
          title: Email
      type: object
      required:
      - id
      - name
      - email
      title: LockedByUser
      description: LockedByUser response to provide user information.
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    DbNarrativeAction:
      type: string
      enum:
      - retain
      - remove
      - reconstruct
      - keep_on_match
    SerializedFiles:
      properties:
        active:
          $ref: '#/components/schemas/SerializedFile'
        current:
          $ref: '#/components/schemas/SerializedFile'
        metadata:
          $ref: '#/components/schemas/SerializedFile'
      type: object
      required:
      - active
      - current
      - metadata
      title: SerializedFiles
      description: Dataclass for serialized file data.
    StatusFilterOption:
      properties:
        label:
          type: string
          enum:
          - Included
          - Excluded
          title: Label
        status:
          type: string
          enum:
          - included
          - excluded
          title: Status
        code_count:
          type: integer
          title: Code Count
      type: object
      required:
      - label
      - status
      - code_count
      title: StatusFilterOption
      description: Model to represent a status filter option.
    UploadCustomCodesCsvInput:
      properties:
        csv_text:
          type: string
          title: Csv Text
          description: Full CSV contents as UTF-8 text
        filename:
          anyOf:
          - type: string
          - type: 'null'
          title: Filename
      type: object
      required:
      - csv_text
      title: UploadCustomCodesCsvInput
      description: Input model for Custom Code CSV.
    UpdateCustomCodeInput:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        system_id:
          type: string
          format: uuid
          title: System Id
        code:
          type: string
          title: Code
        display:
          type: string
          title: Display
      type: object
      required:
      - id
      - system_id
      - code
      - display
      title: UpdateCustomCodeInput
      description: Input model when updating a config's custom code.
    SectionUpdateInput:
      properties:
        include:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Include
        narrative:
          anyOf:
          - $ref: '#/components/schemas/DbNarrativeAction'
          - type: 'null'
        action:
          anyOf:
          - $ref: '#/components/schemas/DbSectionAction'
          - type: 'null'
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        current_code:
          type: string
          title: Current Code
        new_code:
          anyOf:
          - type: string
          - type: 'null'
          title: New Code
      type: object
      required:
      - current_code
      title: SectionUpdateInput
      description: Input model for updating a section's processing instructions.
    AssociateCodesetResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        included_conditions:
          items:
            type: string
            format: uuid
          type: array
          title: Included Conditions
        condition_name:
          type: string
          title: Condition Name
      type: object
      required:
      - id
      - included_conditions
      - condition_name
      title: AssociateCodesetResponse
      description: Response from adding a code set to a config.
    ValidateCustomCodeInput:
      properties:
        current_code:
          anyOf:
          - type: string
          - type: 'null'
          title: Current Code
        desired_code:
          type: string
          title: Desired Code
      type: object
      required:
      - current_code
      - desired_code
      title: ValidateCustomCodeInput
      description: Input model when validating a config's custom code.
    CreateConfigInput:
      properties:
        condition_id:
          type: string
          format: uuid
          title: Condition Id
      type: object
      required:
      - condition_id
      title: CreateConfigInput
      description: Body required to create a new configuration.
    NarrativeDataLabels:
      properties:
        retain:
          type: string
          const: Keep original
          title: Retain
          default: Keep original
        keep_on_match:
          type: string
          const: Keep on match
          title: Keep On Match
          default: Keep on match
        reconstruct:
          type: string
          const: Reconstruct
          title: Reconstruct
          default: Reconstruct
        remove:
          type: string
          const: Exclude
          title: Remove
          default: Exclude
      type: object
      title: NarrativeDataLabels
      description: Enum class to type the narrative actions possible for the frontend.
    CodeResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        condition_id:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Condition Id
        source:
          items:
            type: string
          type: array
          title: Source
        code:
          type: string
          title: Code
        description:
          type: string
          title: Description
        system_id:
          type: string
          format: uuid
          title: System Id
        system_name:
          type: string
          title: System Name
        status:
          type: string
          enum:
          - Included
          - Excluded
          title: Status
        is_custom:
          type: boolean
          title: Is Custom
        is_trigger_code:
          type: boolean
          title: Is Trigger Code
      type: object
      required:
      - id
      - condition_id
      - source
      - code
      - description
      - system_id
      - system_name
      - status
      - is_custom
      - is_trigger_code
      title: CodeResponse
      description: Code object to return to the client.
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    UploadCustomCodesPreviewItem:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        code:
          type: string
          title: Code
        system_id:
          type: string
          format: uuid
          title: System Id
        system_name:
          type: string
          title: System Name
        display:
          type: string
          title: Display
        row:
          anyOf:
          - type: integer
          - type: 'null'
          title: Row
      type: object
      required:
      - id
      - code
      - system_id
      - system_name
      - display
      title: UploadCustomCodesPreviewItem
      description: Validated CSV row ready for confirmation.
    DbConfigurationSectionProcessing:
      properties:
        include:
          type: boolean
          title: Include
        narrative:
          $ref: '#/components/schemas/DbNarrativeAction'
        action:
          $ref: '#/components/schemas/DbSectionAction'
        name:
          type: string
          title: Name
        code:
          type: string
          title: Code
        versions:
          items:
            type: string
          type: array
          title: Versions
        section_type:
          $ref: '#/components/schemas/DbSectionType'
      type: object
      required:
      - include
      - narrative
      - action
      - name
      - code
      - versions
      - section_type
      title: DbConfigurationSectionProcessing
      description: 'Section Processing instructions for a Configuration.


        `name` is the section''s name.

        `code` is the LOINC code for the section.

        `versions` is a list of versions this section appears in.

        `section_type` is an indicator as to how the section was created.'
